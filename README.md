# FluxTokensKit
Build your app’s design foundation once, and reuse everywhere.

Key Features:

Centralized color palettes (Light, Dark, multi-app)

Typography roles for consistent font usage

Spacing system (XS, SM, MD, LG, XL)

Theme manager for runtime switching

Fully decoupled from UI — reusable across any SwiftUI project
